#include "List.h"
#include <vector>
#include <iostream>
using namespace std;

void main()
{
	List<T> *x;

	Showbegin(T &x);
	Showend(T &x);
	Showlistcnt(T &x);
	MaxList(T &x);
	MinList(T &x);
	
}